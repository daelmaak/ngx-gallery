import { NoMoveClickDirective } from './no-move-click.directive';

describe('NoMoveClickDirective', () => {
  it('should create an instance', () => {
    const directive = new NoMoveClickDirective(null);
    expect(directive).toBeTruthy();
  });
});
